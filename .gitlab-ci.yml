stages:
  - deploy

Deploy to Rescue Server:
  stage: deploy
  script:
    - |
      ssh -o StrictHostKeyChecking=no rescue@10.80.10.89 << 'EOF'
        cd /opt/chat-bot
        git pull
        docker compose up -d --build
      EOF
  only:
    - main
